type TreeNode = {
    val:number;
    left: TreeNode | null,
    right: TreeNode | null,
};

export function mergeTrees(root1: TreeNode | null, root2: TreeNode | null): TreeNode | null {
    return null;
};