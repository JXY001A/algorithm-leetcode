type TreeNode = {
    val:number;
    left: TreeNode | null,
    right: TreeNode | null,
};

export function trimBST(root: TreeNode | null, low: number, high: number): TreeNode | null {
    return null;
};